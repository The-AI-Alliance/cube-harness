[project]
name = "recipe-env-miniwob"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cube-harness",
    "miniwob-cube",
    "cube-browser-tool",
]

[tool.uv]
package = false

[tool.uv.sources]
cube-harness = { path = "../..", editable = true }
miniwob-cube = { path = "../../cubes/miniwob", editable = true }
