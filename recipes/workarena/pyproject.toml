[project]
name = "recipe-env-workarena"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cube-harness",
    "workarena-cube",
]

[tool.uv]
package = false

[tool.uv.sources]
cube-harness = { path = "../..", editable = true }
workarena-cube = { path = "../../cubes/workarena", editable = true }
