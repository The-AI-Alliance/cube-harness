# Things to Fix

1. Follow the same pattern as osworld for `task_metadata` in WAA tasks — no explicit `TaskMetadata`, use `self.metadata` if needed.

2. Ensure the fixes suggested by Nicolas for setter and getter eval functions in osworld are in WAA evaluators as well.
